# loglint

`loglint` — статический линтер для Go, проверяющий **лог-сообщения** на соответствие правилам качества и безопасности:

✅ проверяет, что сообщения начинаются с **строчной буквы**  
✅ сообщения только на **английском языке**  
✅ нет эмодзи и спецсимволов в сообщениях  
✅ нет **утечек чувствительных данных** (пароли, токены и т.п.)  
✅ поддержка логгеров:
- стандартный `log`
- `log/slog`
- `go.uber.org/zap`

---

## 🧠 Цель проекта

Этот линтер помогает находить проблемные места в логах:  
• стиль оформления сообщений  
• формат и читаемость  
• безопасность (не отправлять секреты в логи)

Такой линтер полезен для соблюдения код-стандарта и безопасности в больших Go-проектах.

---

## 📦 Установка

Склонируй репозиторий:

```bash
git clone https://github.com/f1rezy/loglint.git
cd loglint
```

Собери бинарник:

```bash
go build -o loglint ./cmd/loglint
```

## ▶️ Запуск

Чтобы проанализировать весь проект:

```bash
./loglint ./...
```